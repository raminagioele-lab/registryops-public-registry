{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://registryops.example/schema_state_v1.json",
  "title": "Registry Aggregated State v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "asset_ref",
    "first_seen_at",
    "last_observed_at",
    "event_count_total",
    "persistence_duration_days",
    "state_level",
    "last_state_update"
  ],
  "properties": {
    "asset_ref": {
      "type": "string",
      "description": "Abstract, non-personal asset identifier."
    },
    "first_seen_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC timestamp of first observed event."
    },
    "last_observed_at": {
      "type": "string",
      "format": "date-time",
      "description": "UTC timestamp of most recent observed event."
    },
    "event_count_total": {
      "type": "integer",
      "minimum": 0,
      "description": "Total number of events recorded for this asset."
    },
    "persistence_duration_days": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of days between first and last observation."
    },
    "state_level": {
      "type": "string",
      "enum": ["stable", "evolving", "persistent"],
      "description": "Derived stability level based on event history."
    },
    "last_state_update": {
      "type": "string",
      "format": "date-time",
      "description": "UTC timestamp when the aggregated state was last recalculated."
    }
  }
}
